export type TPlace = {
  label?: string;
  id: number;
  name: string;
  city: string;
  country: string;
  countryCode: string;
  latitude: number;
  longitude: number;
  region: string;
  regionCode: string;
};
